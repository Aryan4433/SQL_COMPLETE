use aryan_technology;
select * from employees;

#duplicate data;
select E_Name, Department, Max(ID) from employees group by E_Name, Department having count(*)>1;

select E_Name, Department , count(*) from employees group by E_Name, Department having count(*)>1;

SET SQL_SAFE_UPDATES = 0;
delete from employees where ID in ( select ID from (select ID, row_number() over(partition by E_Name,Department,Age) as rnk from employees)as x where rnk > 1);

delete from employees where ID in ( select ID from (select ID, row_number() over(partition by E_name, Department) as rnk from employees) as x where rnk > 1);





























































































































































































